# 🚀 CC-Insight マスター設計書
## RPG日報プラットフォーム - 完全ガイド

**最終更新: 2026/01/06**
**ビジョン: メンバーが熱狂する、RPG型ゲーミフィケーション会員制プラットフォーム**

---

# 📋 SECTION 1: 現在の成功状態

## 1.1 技術スタック
| 技術 | バージョン/詳細 |
|------|----------------|
| Framework | Next.js 14 (App Router) |
| Language | TypeScript |
| Styling | Tailwind CSS + Glassmorphism + Neon Effects |
| Database | Firebase Firestore |
| Authentication | Firebase Auth |
| Hosting | Vercel |
| Repository | GitHub |

## 1.2 Firebase設定（本番稼働中）
```javascript
// Firebase設定は環境変数で管理
// Vercel環境変数: NEXT_PUBLIC_FIREBASE_* を参照
// ローカル開発: .env.local ファイルに設定
```

## 1.3 現在の実装状況
```
cc-insight/
├── app/
│   ├── page.tsx                    # トップページ
│   ├── login/page.tsx              # ログイン画面 ✅ 完成
│   ├── register/page.tsx           # 新規登録画面 ✅ 完成
│   ├── verify-email/page.tsx       # メール認証待ち ✅ 完成
│   ├── pending-approval/page.tsx   # 承認待ち画面 ✅ 完成
│   ├── report/page.tsx             # レポート送信 ✅ 動作確認済
│   ├── dashboard/
│   │   ├── page.tsx                # ダッシュボード ✅ バグ修正済
│   │   ├── side-job/page.tsx       # 副業チーム
│   │   ├── resignation/page.tsx    # 退職チーム
│   │   └── smartphone/page.tsx     # 物販チーム
│   ├── team/
│   │   ├── fukugyou/page.tsx       # 副業チーム詳細
│   │   ├── taishoku/page.tsx       # 退職チーム詳細
│   │   └── buppan/page.tsx         # 物販チーム詳細
│   └── ranking/page.tsx            # ランキング
├── components/
│   ├── sidebar.tsx                 # サイドバーナビ
│   ├── client-layout.tsx           # クライアントレイアウト
│   ├── glass-card.tsx              # Glassmorphism UI
│   └── circular-progress.tsx       # 円形プログレス
├── lib/
│   ├── firebase.ts                 # Firebase初期化
│   ├── firestore.ts                # Firestore操作関数
│   └── auth-context.tsx            # 認証コンテキスト ✅ 会員制対応済
└── scripts/
    └── cleanup-firestore.js        # データクリーンアップ
```

## 1.4 連携状況
| サービス | 状態 | URL/詳細 |
|----------|------|----------|
| GitHub | ✅ 接続済 | https://github.com/ccfp0811-lang/cc-insight.git |
| Vercel | ✅ デプロイ成功 | https://cc-insight.vercel.app |
| Firebase | ✅ 接続成功 | プロジェクト: cc-insight |
| Firestore | ✅ データ保存成功 | reports, users コレクション稼働中 |

---

# 🎮 SECTION 2: RPGゲーミフィケーション戦略

## 2.1 コアコンセプト

### 【ビジョン】
**「努力が可視化され、成長が実感できる、RPG型日報プラットフォーム」**

- メンバーは「冒険者」として自分の成長を追跡
- 日々の報告が「経験値」となり、レベルアップ
- チーム全体で「ギルド」を組み、共に成長
- 達成感と競争心を両立させた設計

## 2.2 レベルシステム

### レベル定義
```
Lv.1  ルーキー           🌱    0〜10,000再生
Lv.2  チャレンジャー      ⭐    10,001〜30,000再生
Lv.3  アクティブ         ⭐⭐   30,001〜70,000再生
Lv.4  プロフェッショナル   ⭐⭐⭐⭐ 70,001〜150,000再生
Lv.5  エキスパート       💎    150,001〜300,000再生
Lv.6  マスター          👑    300,001〜500,000再生
Lv.7  レジェンド         🏆    500,001再生以上
```

### レベルアップ演出
- **画面全体がフラッシュ**（ネオンパルス効果）
- **「LEVEL UP!」アニメーション**
- **新しいバッジ解放の通知**
- **次のレベルまでの進捗バー**

## 2.3 バッジ・実績システム

### 獲得可能バッジ
```typescript
const badges = [
  // 継続系
  { id: "streak7", name: "継続の炎", icon: "🔥", condition: "7日連続報告" },
  { id: "streak30", name: "鉄の意志", icon: "💪", condition: "30日連続報告" },
  { id: "streak100", name: "不屈の精神", icon: "⚡", condition: "100日連続報告" },
  
  // 達成系
  { id: "firstViral", name: "ブレイクスルー", icon: "🚀", condition: "初1万再生達成" },
  { id: "viral10k", name: "バズマスター", icon: "💥", condition: "10万再生達成" },
  { id: "complete100", name: "完全達成", icon: "🎯", condition: "目標100%達成" },
  
  // ランキング系
  { id: "top3weekly", name: "トップランナー", icon: "🥇", condition: "週間TOP3入り" },
  { id: "mvp", name: "MVP", icon: "👑", condition: "月間1位" },
  
  // 成長系
  { id: "growth2x", name: "急成長", icon: "📈", condition: "前週比2倍" },
  { id: "growth5x", name: "爆発的成長", icon: "🌟", condition: "前週比5倍" },
  
  // チーム貢献系
  { id: "teamPlayer", name: "チームプレイヤー", icon: "🤝", condition: "チーム達成率向上に貢献" },
];
```

## 2.4 ストリーク（連続投稿）システム

### UI表示
```
┌─────────────────────────────────────────────────────────┐
│  🔥 投稿ストリーク                                       │
│                                                          │
│   ██████████████████████  18日連続！                    │
│                                                          │
│   🏅 あなたの記録: 最長 23日                             │
│   💪 あと 5日 で自己ベスト更新！                         │
│                                                          │
│   📅 今日報告すると 19日目 達成                          │
│   ⚠️  報告しないとストリークが途切れます                 │
└─────────────────────────────────────────────────────────┘
```

## 2.5 ゴール近接効果UI

### 「あと少しで達成！」演出
```
┌─────────────────────────────────────────────────────────┐
│  🎯 今週の目標まであと少し！                              │
│                                                          │
│   📊 再生数                                              │
│   ████████████████████████░░░░░░░░  78%                 │
│   目標: 10,000  |  現在: 7,800  |  あと 2,200！          │
│                                                          │
│   💡 平均ペースなら あと2日 で達成！                      │
│   📈 昨日より +12% ↑ この調子！                          │
│                                                          │
│   ⚡ 達成まで: あと 2,200再生                            │
└─────────────────────────────────────────────────────────┘
```

### プログレスバーの色変化
```css
/* 達成率に応じた演出 */
0-30%:   #6b7280  グレー（まだまだ）
31-60%:  #06b6d4  シアン（いい感じ）
61-80%:  #eab308  イエロー（もう少し！）
81-99%:  #ec4899  ピンク + パルス（あと少し！）
100%:    #22c55e  ゴールド + キラキラ（達成！🎉）
```

---

# ⚡ SECTION 3: 管理者機能設計

## 3.1 インテリジェント管理ダッシュボード

### URL構造
```
/admin                      # 管理者ダッシュボード（要対応サマリー）
├── /admin/users            # ユーザー管理
│   ├── 承認待ち一覧
│   ├── アクティブユーザー一覧
│   └── ユーザー検索
├── /admin/member/[id]      # 個人詳細パフォーマンス
│   ├── 週次推移グラフ
│   ├── 月次推移グラフ
│   └── 全期間データ
└── /admin/analytics        # 高度な分析
    ├── チーム比較
    └── トレンド分析
```

### トップダッシュボード構成
```
┌─────────────────────────────────────────────────────────────┐
│  📊 管理者ダッシュボード            2026/01/06 16:50       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ⚠️ 要対応 (8件)                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 🔴 承認待ち: 2名         [今すぐ確認]                │  │
│  │ 🟠 3日以上未報告: 5名    [フォローアップ]            │  │
│  │ 🟡 目標未達(50%以下): 3名 [要サポート]               │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  🏆 今週のMVP候補                                          │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 1. 田中さん (副業)  45,230再生  前週比+156% 🔥       │  │
│  │ 2. 佐藤さん (退職)  38,100再生  前週比+89%  📈       │  │
│  │ 3. 鈴木さん (物販)  32,500再生  前週比+67%           │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 3.2 高度な検索・フィルタ機能

### スマートフィルタ（ワンクリック）
```typescript
type SmartFilter = 
  | "needs_follow"     // 要フォロー: 達成率50%以下 or 3日以上未報告
  | "mvp_candidate"    // MVP候補: 達成率100%以上 or 前週比150%以上
  | "new_members"      // 新規メンバー: 登録1週間以内
  | "approval_pending" // 承認待ち
  | "high_achievers"   // ハイパフォーマー: 達成率120%以上
  | "at_risk";         // リスク: 達成率30%以下 or 5日以上未報告
```

### 検索条件インターフェース
```typescript
interface AdminSearchFilter {
  // 基本検索
  keyword?: string;              // 名前・メール検索
  teams?: string[];              // 複数チーム選択
  status?: ("pending" | "approved" | "suspended")[];
  
  // パフォーマンスフィルタ
  achievementRate?: {
    min: number;                 // 達成率 ○% 以上
    max: number;                 // 達成率 ○% 以下
  };
  
  growthRate?: {
    type: "up" | "down" | "all";
    threshold: number;           // 前週比 ○% 以上/以下
  };
  
  lastReportDays?: number;       // 最終報告から ○日以上
  
  // ソート
  sortBy: "name" | "views" | "achievement" | "growth" | "lastReport";
  sortOrder: "asc" | "desc";
}
```

### ドリルダウン機能
- **名前検索 → 個人詳細へ瞬時遷移**
- **個人ページで全データ表示**
  - 週次・月次・クォーター別推移
  - レベル・バッジ表示
  - ストリーク記録

---

# 🗄️ SECTION 4: データベース設計

## 4.1 コレクション構造

### `teams` コレクション（動的管理）
```typescript
interface Team {
  id: string;                    // "fukugyou" | "taishoku" | "buppan"
  name: string;                  // "副業チーム"
  color: string;                 // "#ec4899"
  icon: string;                  // "💼"
  type: "shorts" | "x" | "custom";
  isActive: boolean;             // 有効/無効
  displayOrder: number;          // 表示順
  
  // 目標設定
  goals: {
    dailyPosts: number;          // 日次投稿目標
    weeklyViews?: number;        // 週次再生数目標
    monthlyFollowers?: number;   // 月次フォロワー目標
  };
  
  // カスタム指標（柔軟な設計）
  metrics: Array<{
    key: string;                 // "igViews"
    label: string;               // "IG再生数"
    type: "number" | "text";
    required: boolean;
  }>;
  
  createdAt: Timestamp;
  createdBy: string;             // 作成した管理者UID
}
```

### `users` コレクション
```typescript
interface User {
  uid: string;                   // Firebase Auth UID
  email: string;
  displayName: string;
  teamId: string;                // 動的チーム参照
  role: "member" | "admin";
  status: "pending" | "approved" | "suspended";
  emailVerified: boolean;
  
  // ゲーミフィケーション
  level: number;                 // 現在のレベル
  totalViews: number;            // 累計再生数（経験値）
  badges: string[];              // 獲得バッジID配列
  currentStreak: number;         // 現在の連続日数
  longestStreak: number;         // 最長連続記録
  
  // メタデータ
  createdAt: Timestamp;
  approvedAt?: Timestamp;
  approvedBy?: string;
  lastLoginAt?: Timestamp;
  lastReportAt?: Timestamp;      // 最終報告日時
  profileImage?: string;
}
```

### `reports` コレクション
```typescript
interface Report {
  id: string;
  userId: string;                // ユーザー紐付け
  userEmail: string;
  teamId: string;                // 動的チーム参照
  teamType: "shorts" | "x";
  name: string;
  date: string;                  // YYYY-MM-DD
  createdAt: Timestamp;
  
  // 動的データフィールド
  data: {
    [key: string]: number | string | string[];
  };
  
  // 標準フィールド（後方互換性）
  igViews?: number;
  igProfileAccess?: number;
  postCount?: number;
  likeCount?: number;
  // ... その他既存フィールド
}
```

### `userStats` コレクション（集計用）
```typescript
interface UserStats {
  userId: string;
  period: string;                // "weekly_2026_01" | "monthly_2026_01"
  teamId: string;
  
  // 集計データ
  totalViews: number;
  totalPosts: number;
  totalInteractions: number;
  achievementRate: number;
  growthRate: number;            // 前期比
  
  // ランキング
  teamRank: number;
  overallRank: number;
  
  updatedAt: Timestamp;
}
```

### `badgeAwards` コレクション（バッジ授与記録）
```typescript
interface BadgeAward {
  id: string;
  userId: string;
  badgeId: string;
  awardedAt: Timestamp;
  criteria: string;              // 獲得条件の記録
}
```

### `approvalLogs` コレクション
```typescript
interface ApprovalLog {
  id: string;
  targetUserId: string;
  action: "approve" | "reject" | "suspend" | "reactivate";
  performedBy: string;
  performedAt: Timestamp;
  reason?: string;
}
```

## 4.2 Firestore セキュリティルール

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isApproved() {
      return isAuthenticated() && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.status == "approved";
    }
    
    function isAdmin() {
      return isAuthenticated() && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin";
    }
    
    // Users
    match /users/{userId} {
      allow read: if isAuthenticated() && (request.auth.uid == userId || isAdmin());
      allow create: if isAuthenticated() && request.auth.uid == userId;
      allow update: if isAuthenticated() && request.auth.uid == userId
                    && !request.resource.data.diff(resource.data).affectedKeys()
                        .hasAny(['status', 'role', 'level', 'totalViews']);
      allow update: if isAdmin(); // 管理者は全フィールド更新可能
    }
    
    // Teams（読み取りは全員、編集は管理者のみ）
    match /teams/{teamId} {
      allow read: if isApproved();
      allow write: if isAdmin();
    }
    
    // Reports
    match /reports/{reportId} {
      allow create: if isApproved();
      allow read: if isApproved();
      allow update, delete: if isAdmin();
    }
    
    // UserStats
    match /userStats/{statId} {
      allow read: if isApproved();
      allow write: if isAdmin(); // 集計は管理者のみ
    }
    
    // BadgeAwards
    match /badgeAwards/{awardId} {
      allow read: if isApproved();
      allow write: if isAdmin();
    }
    
    // ApprovalLogs（管理者のみ）
    match /approvalLogs/{logId} {
      allow read, write: if isAdmin();
    }
  }
}
```

---

# 🛤️ SECTION 5: 実装ロードマップ

## Phase 1: 認証・承認基盤（優先度: 最高）✅ 実装完了

- [x] Firebase Auth メール/パスワード認証有効化
- [x] `/register` ページ実装
- [x] `/login` ページ改修
- [x] `/verify-email` ページ実装
- [x] `/pending-approval` ページ実装
- [x] `lib/auth-context.tsx` 大幅改修
- [x] 管理者画面統計バグ修正

**推定工数: 3時間 | 実績: 3時間**

---

## Phase 2: ゲーミフィケーション基盤（優先度: 最高）

### Step 2.1: レベルシステム実装
- [ ] `lib/gamification.ts` 新規作成
- [ ] レベル計算ロジック実装
- [ ] レベルアップ検知・通知
- [ ] マイページにレベル表示

### Step 2.2: バッジシステム実装
- [ ] バッジ定義マスタ作成
- [ ] バッジ獲得条件チェックロジック
- [ ] バッジ表示UI（マイページ）
- [ ] バッジ獲得アニメーション

### Step 2.3: ストリークシステム
- [ ] 連続投稿日数計算ロジック
- [ ] ストリーク表示UI
- [ ] ストリーク途切れアラート

**推定工数: 4時間**

---

## Phase 3: マイページ実装（優先度: 高）

### Step 3.1: ページ構造
- [ ] `/mypage` 基本実装
- [ ] `/mypage/weekly` 週次詳細
- [ ] `/mypage/monthly` 月次詳細
- [ ] レベル・バッジ表示エリア

### Step 3.2: データ取得
- [ ] `getMyReports()` 実装
- [ ] 期間別集計関数
- [ ] 成長率計算

### Step 3.3: UI コンポーネント
- [ ] ネオン調折れ線グラフ（recharts）
- [ ] レベル進捗ゲージ
- [ ] バッジコレクション表示
- [ ] ストリークカウンター

**推定工数: 3時間**

---

## Phase 4: 管理者機能実装（優先度: 高）

### Step 4.1: 管理画面
- [ ] `/admin` ダッシュボード実装
- [ ] `/admin/users` ユーザー管理
- [ ] `/admin/member/[id]` 個人詳細

### Step 4.2: 検索・フィルタ
- [ ] スマートフィルタ実装
- [ ] 高度検索機能
- [ ] ドリルダウン機能

### Step 4.3: 承認・管理
- [ ] ワンクリック承認UI
- [ ] 一括操作機能
- [ ] 承認ログ記録

**推定工数: 4時間**

---

## Phase 5: チーム動的管理（優先度: 中）

### Step 5.1: Teams Collection
- [ ] `teams` コレクション作成
- [ ] チーム管理画面実装
- [ ] チーム追加・編集UI

### Step 5.2: 既存コードリファクタ
- [ ] ハードコードされたteams配列削除
- [ ] 動的チーム取得に変更
- [ ] レポートフォーム動的生成

**推定工数: 2時間**

---

## Phase 6: テスト・デプロイ（優先度: 最高）

### Step 6.1: ローカルテスト
- [ ] 全認証フロー確認
- [ ] ゲーミフィケーション動作確認
- [ ] 管理画面機能確認

### Step 6.2: Firestore設定
- [ ] セキュリティルール更新
- [ ] インデックス作成
- [ ] ルールテスト実行

### Step 6.3: 本番デプロイ
- [ ] Vercel本番デプロイ
- [ ] Firebase本番環境確認
- [ ] 動作確認

**推定工数: 1時間**

---

# 📊 総推定工数

| Phase | 内容 | 推定時間 | 状態 |
|-------|------|----------|------|
| Phase 1 | 認証・承認基盤 | 3h | ✅ 完了 |
| Phase 2 | ゲーミフィケーション | 4h | ⏳ 次タスク |
| Phase 3 | マイページ | 3h | 🔜 予定 |
| Phase 4 | 管理者機能 | 4h | 🔜 予定 |
| Phase 5 | チーム動的管理 | 2h | 🔜 予定 |
| Phase 6 | テスト・デプロイ | 1h | 🔜 予定 |
| **合計** | | **17時間** | |

---

# 🎨 デザインシステム

## カラーパレット
```css
/* チームカラー */
--color-fukugyou: #ec4899;    /* ピンク */
--color-taishoku: #06b6d4;    /* シアン */
--color-buppan: #eab308;      /* イエロー */

/* ネオングロー */
--glow-pink: 0 0 20px rgba(236, 72, 153, 0.5);
--glow-cyan: 0 0 20px rgba(6, 182, 212, 0.5);
--glow-yellow: 0 0 20px rgba(234, 179, 8, 0.5);

/* レベルカラー */
--level-rookie: #6b7280;      /* グレー */
--level-active: #06b6d4;      /* シアン */
--level-pro: #a855f7;         /* パープル */
--level-expert: #ec4899;      /* ピンク */
--level-master: #f59e0b;      /* ゴールド */
--level-legend: #ef4444;      /* レッド */

/* 背景 */
--bg-dark: #0a0a0a;
--bg-card: rgba(255, 255, 255, 0.05);
```

## アニメーション
```css
/* レベルアップフラッシュ */
@keyframes levelup-flash {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.4; background: rgba(236, 72, 153, 0.2); }
}

/* バッジ獲得 */
@keyframes badge-popup {
  0% { transform: scale(0) rotate(0deg); }
  50% { transform: scale(1.2) rotate(180deg); }
  100% { transform: scale(1) rotate(360deg); }
}

/* ストリークパルス */
@keyframes streak-pulse {
  0%, 100% { box-shadow: 0 0 10px rgba(239, 68, 68, 0.5); }
  50% { box-shadow: 0 0 30px rgba(239, 68, 68, 1); }
}
```

---

# ⚠️ 重要な注意事項

1. **統計バグ修正済み** - ダッシュボードの期間フィルタが正しく適用されるように修正
2. **認証フロー完成** - 登録 → メール認証 → 承認待ち → 承認完了のフロー実装済み
3. **拡張性重視** - チーム動的管理で将来の拡張に対応
4. **ゲーミフィケーション必須** - レベル・バッジ・ストリークは全実装予定

---

# 📞 リソース

| リソース | URL |
|----------|-----|
| 本番サイト | https://cc-insight.vercel.app |
| GitHub | https://github.com/ccfp0811-lang/cc-insight.git |
| Firebase Console | https://console.firebase.google.com/project/cc-insight |
| Vercel Dashboard | https://vercel.com |

---

**次のタスク: Phase 1完了後、Firebase Consoleで「メール/パスワード認証」を有効化し、Phase 2へ進む**

*最終更新: 2026/01/06 - RPG日報プラットフォーム戦略統合版*
